const VALID_METHODS = new Set(['GET','POST','PUT','PATCH','DELETE']); function isValidRule(r: any): boolean { if (typeof r !== 'object' || r === null || Array.isArray(r)) return false; if (typeof r.method !== 'string' || !VALID_METHODS.has(r.method)) return false; if (typeof r.path !== 'string') return false; if (!r.path.startsWith('/')) return false; if (r.path.includes('?') || r.path.includes('#')) return false; if (typeof r.status !== 'number' || !Number.isInteger(r.status) || r.status < 200 || r.status > 599) return false; if (!('body' in r)) return false; if ((r.status === 204 || r.status === 304) && r.body !== null) return false; return true; } function processInput(input: any): any { if (typeof input !== 'object' || input === null || Array.isArray(input)) return { error: 'CONFIG' }; const routes = input.routes; const requests = input.requests; if (!Array.isArray(routes) || !Array.isArray(requests)) return { error: 'CONFIG' }; const map = new Map<string, { status: number; body: any }>(); const seen = new Set<string>(); for (const rule of routes) { if (!isValidRule(rule)) return { error: 'CONFIG' }; const key = JSON.stringify([rule.method, rule.path]); if (seen.has(key)) return { error: 'DUPLICATE_ROUTE' }; seen.add(key); map.set(key, { status: rule.status, body: rule.body }); } const responses = requests.map((req: any) => { let status = 404; let body: any = { error: 'NOT_FOUND' }; if (req && typeof req === 'object' && typeof req.method === 'string' && typeof req.target === 'string') { let target = req.target; const hash = target.indexOf('#'); if (hash >= 0) target = target.slice(0, hash); const q = target.indexOf('?'); if (q >= 0) target = target.slice(0, q); const key = JSON.stringify([req.method, target]); const found = map.get(key); if (found) { status = found.status; body = found.body; } } return { status, body }; }); return { responses }; } const NEWLINE = String.fromCharCode(10); function handleLine(line: string) { let input: any; try { input = JSON.parse(line); } catch { console.log(JSON.stringify({ error: 'INVALID_JSON' })); return; } const output = processInput(input); console.log(JSON.stringify(output)); } const decoder = new TextDecoder(); let buffer = ''; for await (const chunk of Bun.stdin.stream()) { buffer += decoder.decode(chunk, { stream: true }); let idx: number; while ((idx = buffer.indexOf(NEWLINE)) >= 0) { const line = buffer.slice(0, idx); buffer = buffer.slice(idx + 1); handleLine(line); } } buffer += decoder.decode(); if (buffer.length > 0) { handleLine(buffer); }
