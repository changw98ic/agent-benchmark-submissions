{"files":[{"path":"main.cpp","content":"#include <iostream>\n#include <string>\n#include <vector>\n#include <cstdint>\n\nusing namespace std;\n\nstatic string jsonEscape(const string& s) {\n    string out;\n    out.reserve(s.size() + 2);\n    for (char c : s) {\n        unsigned char u = (unsigned char)c;\n        if (c == '\"') out += \"\\\\\\\"\";\n        else if (c == '\\\\') out += \"\\\\\\\\\";\n        else if (c == '\\n') out += \"\\\\n\";\n        else if (c == '\\r') out += \"\\\\r\";\n        else if (c == '\\t') out += \"\\\\t\";\n        else if (u < 0x20) {\n            char buf[8];\n            snprintf(buf, sizeof(buf), \"\\\\u%04x\", u);\n            out += buf;\n        } else out += c;\n    }\n    return out;\n}\n\nstatic string hexLower(const uint8_t* p, size_t n) {\n    static const char* d = \"0123456789abcdef\";\n    string out;\n    out.reserve(n * 2);\n    for (size_t i = 0; i < n; i++) {\n        out += d[p[i] >> 4];\n        out += d[p[i] & 0x0f];\n    }\n    return out;\n}\n\nstatic int hexVal(char c) {\n    if (c >= '0' && c <= '9') return c - '0';\n    if (c >= 'a' && c <= 'f') return c - 'a' + 10;\n    if (c >= 'A' && c <= 'F') return c - 'A' + 10;\n    return -1;\n}\n\n// Minimal parser: extract string value of key \"hex\" from a flat JSON object.\n// Returns false on malformed JSON.\nstatic bool extractHexField(const string& line, string& hex) {\n    size_t i = 0, n = line.size();\n    while (i < n && (unsigned char)line[i] <= ' ') i++;\n    if (i >= n || line[i] != '{') return false;\n    i++;\n    while (i < n) {\n        while (i < n && (unsigned char)line[i] <= ' ') i++;\n        if (i < n && line[i] == '}') return true;\n        if (i >= n || line[i] != '\"') return false;\n        i++;\n        string key;\n        bool closed = false;\n        while (i < n) {\n            char c = line[i];\n            if (c == '\\\\') {\n                if (i + 1 >= n) return false;\n                char e = line[i + 1];\n                if (e == 'n') key += '\\n';\n                else if (e == 't') key += '\\t';\n                else if (e == 'r') key += '\\r';\n                else if (e == 'b') key += '\\b';\n                else if (e == 'f') key += '\\f';\n                else if (e == '\"' || e == '\\\\' || e == '/') key += e;\n                else if (e == 'u') {\n                    if (i + 5 >= n) return false;\n                    unsigned v = 0;\n                    for (int k = 2; k <= 5; k++) {\n                        int hv = hexVal(line[i + k]);\n                        if (hv < 0) return false;\n                        v = (v << 4) | (unsigned)hv;\n                    }\n                    if (v < 0x80) key += (char)v;\n                    else key += '?';\n                    i += 6;\n                    continue;\n                } else return false;\n                i += 2;\n                continue;\n            }\n            if (c == '\"') { closed = true; i++; break; }\n            key += c;\n            i++;\n        }\n        if (!closed) return false;\n        while (i < n && (unsigned char)line[i] <= ' ') i++;\n        if (i >= n || line[i] != ':') return false;\n        i++;\n        while (i < n && (unsigned char)line[i] <= ' ') i++;\n        if (i >= n) return false;\n        if (line[i] != '\"') {\n            // skip non-string value (number/bool/null/object/array) up to , or }\n            int depth = 0;\n            bool instr = false;\n            while (i < n) {\n                char c = line[i];\n                if (instr) {\n                    if (c == '\\\\') { i += 2; continue; }\n                    if (c == '\"') instr = false;\n                } else {\n                    if (c == '\"') instr = true;\n                    else if (c == '{' || c == '[') depth++;\n                    else if (c == '}' || c == ']') {\n                        if (depth == 0) break;\n                        depth--;\n                    } else if (c == ',' && depth == 0) break;\n                }\n                i++;\n            }\n        } else {\n            i++;\n            string val;\n            bool vclosed = false;\n            while (i < n) {\n                char c = line[i];\n                if (c == '\\\\') {\n                    if (i + 1 >= n) return false;\n                    char e = line[i + 1];\n                    if (e == 'n') val += '\\n';\n                    else if (e == 't') val += '\\t';\n                    else if (e == 'r') val += '\\r';\n                    else if (e == 'b') val += '\\b';\n                    else if (e == 'f') val += '\\f';\n                    else if (e == '\"' || e == '\\\\' || e == '/') val += e;\n                    else if (e == 'u') {\n                        if (i + 5 >= n) return false;\n                        unsigned v = 0;\n                        for (int k = 2; k <= 5; k++) {\n                            int hv = hexVal(line[i + k]);\n                            if (hv < 0) return false;\n                            v = (v << 4) | (unsigned)hv;\n                        }\n                        if (v < 0x80) val += (char)v;\n                        else val += '?';\n                        i += 6;\n                        continue;\n                    } else return false;\n                    i += 2;\n                    continue;\n                }\n                if (c == '\"') { vclosed = true; i++; break; }\n                val += c;\n                i++;\n            }\n            if (!vclosed) return false;\n            if (key == \"hex\") hex = val;\n        }\n        while (i < n && (unsigned char)line[i] <= ' ') i++;\n        if (i < n && line[i] == ',') { i++; continue; }\n        if (i < n && line[i] == '}') return true;\n        if (i >= n) return true;\n        return false;\n    }\n    return true;\n}\n\nstruct Rec {\n    int type;\n    bool isInt;\n    int ival;\n    string sval;\n};\n\nstatic string process(const string& line) {\n    string hex;\n    if (!extractHexField(line, hex)) return \"{\\\"error\\\":\\\"INVALID_HEX\\\"}\";\n    if (hex.size() % 2 != 0) return \"{\\\"error\\\":\\\"INVALID_HEX\\\"}\";\n    vector<uint8_t> data;\n    data.reserve(hex.size() / 2);\n    for (size_t i = 0; i < hex.size(); i += 2) {\n        int hi = hexVal(hex[i]), lo = hexVal(hex[i + 1]);\n        if (hi < 0 || lo < 0) return \"{\\\"error\\\":\\\"INVALID_HEX\\\"}\";\n        data.push_back((uint8_t)((hi << 4) | lo));\n    }\n\n    vector<Rec> recs;\n    size_t pos = 0, n = data.size();\n    while (pos < n) {\n        size_t start = pos;\n        if (n - pos < 3) {\n            return \"{\\\"error\\\":\\\"TRUNCATED\\\",\\\"offset\\\":\" + to_string(start) + \"}\";\n        }\n        uint8_t type = data[pos];\n        uint32_t len = ((uint32_t)data[pos + 1] << 8) | (uint32_t)data[pos + 2];\n        pos += 3;\n        if (n - pos < len) {\n            return \"{\\\"error\\\":\\\"TRUNCATED\\\",\\\"offset\\\":\" + to_string(start) + \"}\";\n        }\n        const uint8_t* payload = data.data() + pos;\n\n        if (type == 1) {\n            if (len < 1 || len > 32) {\n                return \"{\\\"error\\\":\\\"INVALID_VALUE\\\",\\\"offset\\\":\" + to_string(start) + \"}\";\n            }\n            bool ok = true;\n            for (uint32_t k = 0; k < len; k++) {\n                if (payload[k] < 32 || payload[k] > 126) { ok = false; break; }\n            }\n            if (!ok) {\n                return \"{\\\"error\\\":\\\"INVALID_VALUE\\\",\\\"offset\\\":\" + to_string(start) + \"}\";\n            }\n            Rec r;\n            r.type = 1;\n            r.isInt = false;\n            r.ival = 0;\n            r.sval.assign((const char*)payload, len);\n            recs.push_back(std::move(r));\n        } else if (type == 2) {\n            if (len != 4) {\n                return \"{\\\"error\\\":\\\"INVALID_VALUE\\\",\\\"offset\\\":\" + to_string(start) + \"}\";\n            }\n            uint32_t u = ((uint32_t)payload[0] << 24) | ((uint32_t)payload[1] << 16) |\n                         ((uint32_t)payload[2] << 8) | (uint32_t)payload[3];\n            int32_t v = (int32_t)u;\n            Rec r;\n            r.type = 2;\n            r.isInt = true;\n            r.ival = (int)v;\n            r.sval = to_string((long long)v);\n            recs.push_back(std::move(r));\n        } else {\n            Rec r;\n            r.type = (int)type;\n            r.isInt = false;\n            r.ival = 0;\n            r.sval = hexLower(payload, len);\n            recs.push_back(std::move(r));\n        }\n        pos += len;\n    }\n\n    string out = \"{\\\"records\\\":[\";\n    for (size_t i = 0; i < recs.size(); i++) {\n        if (i) out += ',';\n        out += \"{\\\"type\\\":\" + to_string(recs[i].type) + \",\\\"value\\\":\";\n        if (recs[i].isInt) out += recs[i].sval;\n        else out += '\"' + jsonEscape(recs[i].sval) + '\"';\n        out += '}';\n    }\n    out += \"]}\";\n    return out;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    string line;\n    while (getline(cin, line)) {\n        if (line.empty()) continue;\n        cout << process(line) << '\\n';\n        cout.flush();\n    }\n    return 0;\n}\n"},"notes":"JSONL line-per-request TLV parser: hex decode, bounds-checked cursor, ASCII/int32/opaque value rules, first-error response with byte offset, ordered records output."}
