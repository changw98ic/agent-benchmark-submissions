"content": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Json {\n    enum Type { Null, Bool, Number, String, Array, Object } type = Null;\n    long long num = 0;\n    bool boolean = false;\n    string str;\n    vector<Json> arr;\n    vector<pair<string, Json>> obj;\n};\n\nvoid printError(const string& msg) {\n    cout << '{' << '\"' << \"error\" << '\"' << ':' << '\"' << msg << '\"' << '}' << char(10);\n}\n\nclass Parser {\n    string_view s;\n    size_t i = 0;\npublic:\n    Parser(string_view sv) : s(sv) {}\n    void skipWs() {\n        while (i < s.size() && isspace(static_cast<unsigned char>(s[i]))) i++;\n    }\n    bool parse(Json& out) {\n        skipWs();\n        if (!parseValue(out)) return false;\n        skipWs();\n        return i == s.size();\n    }\n    bool parseValue(Json& out) {\n        skipWs();\n        if (i >= s.size()) return false;\n        char c = s[i];\n        if (c == '{') return parseObject(out);\n        if (c == '[') return parseArray(out);\n        if (c == '\"') { out.type = Json::String; return parseString(out.str); }\n        if (c == '-' || isdigit(static_cast<unsigned char>(c))) { out.type = Json::Number; return parseNumber(out.num); }\n        if (s.compare(i, 4, \"true\") == 0) { i += 4; out.type = Json::Bool; out.boolean = true; return true; }\n        if (s.compare(i, 5, \"false\") == 0) { i += 5; out.type = Json::Bool; out.boolean = false; return true; }\n        if (s.compare(i, 4, \"null\") == 0) { i += 4; out.type = Json::Null; return true; }\n        return false;\n    }\n    bool parseObject(Json& out) {\n        out.type = Json::Object;\n        if (i >= s.size() || s[i] != '{') return false;\n        i++;\n        skipWs();\n        if (i < s.size() && s[i] == '}') { i++; return true; }\n        while (true) {\n            skipWs();\n            if (i >= s.size() || s[i] != '\"') return false;\n            string key;\n            if (!parseString(key)) return false;\n            skipWs();\n            if (i >= s.size() || s[i] != ':') return false;\n            i++;\n            Json val;\n            if (!parseValue(val)) return false;\n            out.obj.emplace_back(move(key), move(val));\n            skipWs();\n            if (i >= s.size()) return false;\n            if (s[i] == ',') { i++; continue; }\n            if (s[i] == '}') { i++; break; }\n            return false;\n        }\n        return true;\n    }\n    bool parseArray(Json& out) {\n        out.type = Json::Array;\n        if (i >= s.size() || s[i] != '[') return false;\n        i++;\n        skipWs();\n        if (i < s.size() && s[i] == ']') { i++; return true; }\n        while (true) {\n            Json val;\n            if (!parseValue(val)) return false;\n            out.arr.push_back(move(val));\n            skipWs();\n            if (i >= s.size()) return false;\n            if (s[i] == ',') { i++; continue; }\n            if (s[i] == ']') { i++; break; }\n            return false;\n        }\n        return true;\n    }\n    bool parseString(string& out) {\n        if (i >= s.size() || s[i] != '\"') return false;\n        i++;\n        out.clear();\n        while (i < s.size()) {\n            char c = s[i++];\n            if (c == '\"') return true;\n            if (c == 92) {\n                if (i >= s.size()) return false;\n                char esc = s[i++];\n                switch (esc) {\n                    case 34: out.push_back(34); break;\n                    case 92: out.push_back(92); break;\n                    case 47: out.push_back(47); break;\n                    case 98: out.push_back(8); break;\n                    case 102: out.push_back(12); break;\n                    case 110: out.push_back(10); break;\n                    case 114: out.push_back(13); break;\n                    case 116: out.push_back(9); break;\n                    default: return false;\n                }\n            } else {\n                out.push_back(c);\n            }\n        }\n        return false;\n    }\n    bool parseNumber(long long& out) {\n        size_t start = i;\n        if (i < s.size() && s[i] == '-') i++;\n        if (i >= s.size() || !isdigit(static_cast<unsigned char>(s[i]))) return false;\n        while (i < s.size() && isdigit(static_cast<unsigned char>(s[i]))) i++;\n        if (i < s.size() && (s[i] == '.' || s[i] == 'e' || s[i] == 'E')) return false;\n        string num(s.substr(start, i - start));\n        try {\n            out = stoll(num);\n        } catch (...) {\n            return false;\n        }\n        return true;\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    string line;\n    while (getline(cin, line)) {\n        if (!line.empty() && line.back() == 13) line.pop_back();\n        if (line.empty()) continue;\n        Parser p(line);\n        Json root;\n        if (!p.parse(root) || root.type != Json::Object) {\n            printError(\"INVALID_INPUT\");\n            continue;\n        }\n        const Json* intervalsJson = nullptr;\n        for (auto& kv : root.obj) if (kv.first == \"intervals\") { intervalsJson = &kv.second; break; }\n        if (!intervalsJson || intervalsJson->type != Json::Array) {\n            printError(\"INVALID_INPUT\");\n            continue;\n        }\n        vector<pair<long long, long long>> intervals;\n        bool invalid = false;\n        for (const auto& item : intervalsJson->arr) {\n            if (item.type != Json::Array || item.arr.size() != 2) { invalid = true; break; }\n            if (item.arr[0].type != Json::Number || item.arr[1].type != Json::Number) { invalid = true; break; }\n            long long s = item.arr[0].num;\n            long long e = item.arr[1].num;\n            if (s > e) { invalid = true; break; }\n            if (s == e) continue;\n            intervals.push_back({s, e});\n        }\n        if (invalid) {\n            printError(\"INVALID_INTERVAL\");\n            continue;\n        }\n        const Json* clipJson = nullptr;\n        for (auto& kv : root.obj) if (kv.first == \"clip\") { clipJson = &kv.second; break; }\n        if (clipJson) {\n            if (clipJson->type != Json::Array || clipJson->arr.size() != 2) {\n                printError(\"INVALID_CLIP\");\n                continue;\n            }\n            if (clipJson->arr[0].type != Json::Number || clipJson->arr[1].type != Json::Number) {\n                printError(\"INVALID_CLIP\");\n                continue;\n            }\n            long long lo = clipJson->arr[0].num;\n            long long hi = clipJson->arr[1].num;\n            if (lo > hi) {\n                printError(\"INVALID_CLIP\");\n                continue;\n            }\n            vector<pair<long long, long long>> clipped;\n            for (auto [s, e] : intervals) {\n                long long ns = max(s, lo);\n                long long ne = min(e, hi);\n                if (ns < ne) clipped.push_back({ns, ne});\n            }\n            intervals = move(clipped);\n        }\n        sort(intervals.begin(), intervals.end());\n        vector<pair<long long, long long>> merged;\n        for (auto [s, e] : intervals) {\n            if (merged.empty()) {\n                merged.push_back({s, e});\n            } else {\n                if (s <= merged.back().second) {\n                    if (e > merged.back().second) merged.back().second = e;\n                } else {\n                    merged.push_back({s, e});\n                }\n            }\n        }\n        long long length = 0;\n        for (auto [s, e] : merged) length += (e - s);\n        cout << '{' << '\"' << \"intervals\" << '\"' << ':' << '[';\n        for (size_t i = 0; i < merged.size(); ++i) {\n            if (i) cout << ',';\n            cout << '[' << merged[i].first << ',' << merged[i].second << ']';\n        }\n        cout << ']' << ',' << '\"' << \"length\" << '\"' << ':' << length << '}' << char(10);\n    }\n    return 0;\n}\n",
